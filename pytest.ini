[pytest]
addopts = -v --tb=short -m "not skip"
testpaths = tests
pythonpath = src
markers =
    e2e: end-to-end browser flows via MCP Playwright Agent
    slow: longer tests
    BVT: Mark a test as a Build Verification Test
    PGSQL: PGSQL style tests
